import { DBtypes } from './DB.types';

const DB: DBtypes = {
  blogs: [
    {
      id: 'string',
      title: 'string',
      shortDescription: 'string',
      content: 'string',
      blogId: 'string',
      blogName: 'string',
    },
  ],
  posts: [
    {
      id: 'string',
      name: 'string',
      description: 'string',
      websiteUrl: 'string',
    },
  ],
};
export default DB;
